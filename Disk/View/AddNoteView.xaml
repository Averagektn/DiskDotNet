<UserControl x:Class="Disk.View.AddNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Disk.View" xmlns:viewmodel="clr-namespace:Disk.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:AddNoteViewModel}"
             xmlns:lang="clr-namespace:Disk.Properties.Langs.AddNote" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Style/BtnStyles.xaml"/>
    </UserControl.Resources>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="{x:Static lang:AddNoteLocalization.Note}" 
               Margin="10,15"
               HorizontalAlignment="Left"/>
        <TextBox Margin="10 0" Padding="0,5"
              Width="255"
              BorderThickness="0,0,0,1"
              HorizontalAlignment="Left"
              Text="{Binding Note.Text}"/>

        <Button Content="{x:Static lang:AddNoteLocalization.Add}" Command="{Binding AddNoteCommand}"                    
                Foreground="White"
                    Background="Black"
                    Margin="0 10"
                    Height="35"
                    Width="250"/>

        <Button Content="{x:Static lang:AddNoteLocalization.Cancel}" Command="{Binding CancelCommand}"                    
                Foreground="White"
                    Background="Black"
                    Height="35"
                    Width="250"/>
    </StackPanel>
</UserControl>
