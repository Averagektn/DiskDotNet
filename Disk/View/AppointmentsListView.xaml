<UserControl x:Class="Disk.View.AppointmentsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:lang="clr-namespace:Disk.Properties.Langs.AppointementsList" 
             xmlns:local="clr-namespace:Disk.View" xmlns:viewmodel="clr-namespace:Disk.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:AppointmentsListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Style/BtnStyles.xaml"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.Surname}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.Surname}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.Name}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.Name}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.Patronymic}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.Patronymic}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.MobilePhone}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.PhoneMobile}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.HomePhone}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.PhoneHome}" Margin="10 0 " FontSize="20"/>
            <TextBlock Text="{x:Static lang:AppointmentsListLocalization.DateOfBirth}" Margin="10 0" FontSize="20"/>
            <TextBlock Text="{Binding Patient.DateOfBirth}" Margin="10 0" FontSize="20"/>
        </StackPanel>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="{x:Static lang:AppointmentsListLocalization.AppoimentsHeader}" Margin="0 10 0 0"/>
                <DataGrid  ScrollViewer.VerticalScrollBarVisibility="Auto" x:Name="AppointmentsDataGrid" 
                    Margin="5 0"
                    ItemsSource="{Binding Appointments}" 
                    AutoGenerateColumns="False" 
                    SelectedItem="{Binding SelectedAppointment}" 
                    IsReadOnly="True"> 
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="1*" Header="{x:Static lang:AppointmentsListLocalization.Doctor}" Binding="{Binding DoctorNavigation}" />
                        <DataGridTextColumn Width="1*" Header="{x:Static lang:AppointmentsListLocalization.DateTime}" Binding="{Binding Time}" />
                    </DataGrid.Columns>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding ToAppointmentCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </DataGrid>

                <Button
                    Content="{x:Static lang:AppointmentsListLocalization.StartAppoiment}" 
                    Foreground="White"
                    Background="Black"
                    Margin="0,25"
                    Height="35"
                    Command="{Binding StartAppointmentCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GradientButtonStyle}"/>
                    </Button.Style>
                </Button>
            </StackPanel>

        </ScrollViewer>
        
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="{x:Static lang:AppointmentsListLocalization.NotesHeader}" Margin="0 10 0 0"/>
                <DataGrid 
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                  Margin="5 0"
                  ItemsSource="{Binding Notes}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="1*" Header="{x:Static lang:AppointmentsListLocalization.Doctor}" Binding="{Binding DoctorNavigation}" />
                        <DataGridTextColumn Width="1*" Header="{x:Static lang:AppointmentsListLocalization.Text}" Binding="{Binding Text}" />
                    </DataGrid.Columns>
                </DataGrid>

                <Button
                    Content="{x:Static lang:AppointmentsListLocalization.AddNote}" 
                    Foreground="White"
                    Background="Black"
                    Margin="0,25"
                    Height="35"
                    Command="{Binding AddNoteCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GradientButtonStyle}"/>
                    </Button.Style>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
